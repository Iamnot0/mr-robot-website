services:
  - type: web
    name: mr-robot-frontend
    env: static
    buildCommand: npm run build
    staticPublishPath: website/fronted/build
    rewrites:
      - source: /*
        destination: /index.html
    envVars:
      - key: NODE_ENV
        value: production
